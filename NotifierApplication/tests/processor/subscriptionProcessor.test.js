// subscriptionProcessor = require('../../processor/subscriptionProcessor');

// jest.mock('../../services/subscriptions');



// // partial = jest.mock('../../processor/subscriptionProcessor', function () {
// //     const originalModule = jest.requireActual('../../processor/subscriptionProcessor');
// //     originalModule.createChannelProcessors = function () { return new Map();},
// //     originalModule.determineChannelMap = function () { return new Map();}
// //     return originalModule
// // });

test('test', () => {

//     subscriptionProcessor.exportFunctions.determineChannelMap = jest.fn().mockReturnValue(new Map());
//     subscriptionProcessor.exportFunctions.createChannelProcessors = jest.fn().mockReturnValue(new Map());
//     //jest.spyOn(subscriptionProcessor, 'createChannelProcessors').mockImplementation(function() {return new Map();});
//     let result = subscriptionProcessor.exportFunctions.getProcessorsForActiveSubscriptions();

//     console.log(result);
//     //verify the outcome
//     expect(result).toEqual(new Map());
});